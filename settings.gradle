pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // esc-pos-printer is hosted on JitPack
        maven { url 'https://jitpack.io' }
    }
}

rootProject.name = "KasirPro"
include ':app'
